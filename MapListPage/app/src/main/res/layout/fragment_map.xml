<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MapFragment">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.60" />


    <!--Dorito Help Button-->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/dorito_button"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:background="@android:color/transparent"
        app:lottie_fileName="help_animation.json"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:layout_constraintBottom_toBottomOf="@id/map"
        app:layout_constraintEnd_toEndOf="@id/map"
        android:paddingBottom="10dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="55dp"/>

    <TextView
        android:id="@+id/help_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Help?"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/warning_badge"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:layout_marginBottom="5sp"
        android:layout_marginEnd="10dp"
        app:layout_constraintStart_toStartOf="@id/dorito_button"
        app:layout_constraintEnd_toEndOf="@id/dorito_button"
        app:layout_constraintBottom_toTopOf="@id/dorito_button"
        app:layout_constraintHorizontal_bias="0.5" />

    <!-- RecyclerView = 35% height -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_events"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="12dp"
        android:clipToPadding="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/map"
        app:layout_constraintHeight_percent="0.30" />

    <!-- Button completely below RecyclerView -->
    <Button
        android:id="@+id/map_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Map all Events Near Me"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@id/recycler_view_events"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="5dp"
        android:paddingBottom="10dp"
        android:onClick="onMapReady"/>

</androidx.constraintlayout.widget.ConstraintLayout>